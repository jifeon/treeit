( function () {

  var name        = 'ofio.sets';
  var dependences = [];
  var nameSpace   = 'sets';

  var module = new function () {

    this.interscet = function ( set1, set2 ) {
      
    };

  };

  new Ofio.Module ( {
    name        : name,
    module      : module,
    dependences : dependences,
    namespace   : nameSpace
  } );

})();